##HOW TO EXECUTE THE GATE PASSER

1. Set up as normal on foxglove and unity
2. May use the bash setup.sh provided if u want
3. colcon build (with symlink also ok) just in-case
4. Run "bash scripts/nav2gate.sh" inside a container on a new terminal


